schurparlett:
profiling
input validation
calculate flops of the 3 parlett recurrence versions (complex and non)
blocchi jordan benchmark
f(x) = x^40
document schurparlett(T, Q)
Taylor termination

krylov:
profile/optimize
intro comment to ratkrylov
input validation
views in ratkrylov? profile
test ratkrylovf
automatic poles selection - aaa
K\H does not work with complex ratkrylov?

aaa:
profile/optimize
what happens when f(x) is NaN/Inf?
distribution of samples? 2D Chebyshev? polynomial interpolation in equispaced points is a bad idea (the Runge phenomenon).
K\H does not work with complex ratkrylov?
